---
description: 
---

# New Item
Steps:
1. Ask: "Item ID? Stack size? Creative tab? Any special behavior or just base Item?"
2. Generate item class only if custom behavior needed; otherwise use `new Item(settings)` inline.
3. Register in `ModItems`.
4. Generate item model JSON and `en_us` lang entry.
5. Show all files labeled separately.